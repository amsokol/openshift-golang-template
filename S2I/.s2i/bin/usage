#!/bin/bash -e
cat <<EOF
This is the amsokol/golang-openshift:1.9.x-x S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build git://<source code> amsokol/golang-openshift:1.9.x-x <application image>

You can then run the resulting image via:
docker run <application image>
EOF
