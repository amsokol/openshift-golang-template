#!/bin/bash
cat <<EOF
This is the sti-labs-golang1 S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

sti build git://<source code> sti-labs-golang1 <application image>

You can then run the resulting image via:
docker run <application image>
EOF
